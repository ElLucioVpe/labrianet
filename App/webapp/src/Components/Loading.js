import React from 'react'

export const Loading = () => {
    let usuario = false;
    return (
        <div class="container">
            <h1>Cargando...</h1>
        </div>
    )
}

export default Loading